## Summary

<!-- What does this PR do and why? -->

## Linked issue

Closes #

## Model

Model:

## ADRs touched

<!-- List ADR(s) this PR follows or supersedes. "None" if not applicable. -->

## Tests run

<!-- e.g. `pnpm lint && pnpm typecheck && pnpm test` -->

## Checklist

- [ ] Branch is `jason/<topic>`, `claude/<topic>`, or `hermes/<topic>`.
- [ ] Exactly one label: `agent:claude-code` (or `agent:hermes`, `agent:human`).
- [ ] `Model:` line above is filled in.
- [ ] Linked issue; ADRs touched listed.
- [ ] Kept `Co-Authored-By` trailers.
- [ ] `pnpm lint && pnpm typecheck && pnpm test` green locally.
- [ ] If fixtures changed: captured by a human, trimmed, anonymised (ADR-0006 §5).
